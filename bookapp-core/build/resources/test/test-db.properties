#global
# Even though we don't use the property in test since we use the embedded database, we need the property because
# the AppDatabasePropsLoader expects the property file to exist under the class path.
jndi.datasource.name=BookappDS